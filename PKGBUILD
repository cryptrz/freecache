pkgname=freecache
pkgver=1.0.0
pkgrel=1
arch=('any')
pkgdesc="Clean the memory cache"
license=('GPL')
source=('freecache.sh')
sha256sums=('SKIP')

package() {
  install -Dm755 "$srcdir/freecache.sh" "$pkgdir/usr/bin/freecache"
}
